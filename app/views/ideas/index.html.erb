<h1>  All Ideas </h1>
<div class = "row">
    <div class= "col-2">
    </div>
    <div class = "col-10">
        <% @ideas.each do |idea|%>
        <div>
            <h2><%= link_to idea.title, idea_path(idea) %></h2>
        </div>
            <p>
                <%= idea.description %>
            </p>
        <% end %>
    </div>
</div>